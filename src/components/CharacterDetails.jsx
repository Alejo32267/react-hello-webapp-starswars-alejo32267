/*import React from "react";

export const CharacterDetails = () => {
   
    // Obtener detalles por uid
const getCharacterDetails = (uid) =>
  fetch(`https://www.swapi.tech/api/people/${uid}`)
    .then(res => res.json())
    .then(data => data.result);
}

export default CharacterDetails; */

